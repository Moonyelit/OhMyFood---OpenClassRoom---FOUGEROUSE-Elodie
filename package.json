{
  "name": "ohmyfood-project",
  "version": "1.0.0",
  "description": "Projet OhMyFood avec animations CSS",
  "scripts": {
    "sass": "sass sass/main.scss:assets/css/main.css --watch",
    "sass:build": "sass sass/main.scss:assets/css/main.css --style=compressed",
    "sass:dev": "sass sass/main.scss:assets/css/main.css --style=expanded --source-map",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "sass": "^1.92.1"
  }
}
